import { MockApi } from './mock-api.service';
import { NgModule } from '@angular/core';

@NgModule({
  imports: [

  ],
  providers: [
  ],
  exports: [
    
  ]
})
export class DomainModule { 
    
}